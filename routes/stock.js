const express = require("express");
const axios = require("axios");
const router = express.Router();
const company =require("../datasets/stockData.js");
const api = require("../datasets/stockData.js");

router.get()

module.exports = router;
